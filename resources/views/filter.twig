<select
        class="custom-select form-control"
        name="{{ field_type.input_name }}">

    <option value="">{{ trans(field_type.placeholder) }}</option>

    {% for value, title in field_type.options %}
        <option value="{{ value }}" {{ value == field_type.key ? 'selected' }}>
            {{ ucwords(str_replace(['-', '_'], ' ', option)) }}
        </option>
    {% endfor %}
</select>



<div  role="treeitem" aria-selected="true">

    500px

</div>


<div role="treeitem"><i class="fa fa-500px"></i>
    500px
</div>
